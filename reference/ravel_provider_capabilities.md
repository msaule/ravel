# Report provider capabilities

Report provider capabilities

## Usage

``` r
ravel_provider_capabilities(
  provider = c("openai", "copilot", "gemini", "anthropic")
)
```

## Arguments

- provider:

  Provider name.

## Value

A named list.
